# RED — сырые данные, ТОЛЬКО локально

Сюда кладётся НЕанонимизированный материал. Этой папки нет в области видимости агента
(`.claudeignore`). Claude Code НЕ должен её читать. Для демо она пуста — мы показываем,
что агент видит только зелёную папку.
